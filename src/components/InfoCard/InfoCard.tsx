// React
import React, { FC, memo } from "react";
// Styles
import styles from './InfoCard.module.css'

const InfoCard: FC = () => {
    return(
        <div>

        </div>
    )
}

export default memo(InfoCard);